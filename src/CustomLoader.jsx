import { Html } from "@react-three/drei";

const CustomLoader = () => {
    return (
        <Html wrapperClass="loader" position={[0, 0]}>
            Loading
        </Html>
    );
};

export default CustomLoader;
